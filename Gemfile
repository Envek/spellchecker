# frozen_string_literal: true

source 'https://rubygems.org'

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

gem 'ffi-hunspell', github: 'Envek/ffi-hunspell', branch: 'add_dic_support_continued'
gem 'json'
gem 'puma'
gem 'rack'

group :test do
  gem 'rack-test'
  gem 'rspec'
end
